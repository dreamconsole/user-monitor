# Server API base URL (no trailing slash). Used for login and syncing data.
# Local dev (./start.sh): use the same host as the CRM backend
API_URL=http://localhost:3000
# Production:
# API_URL=https://api.sourcecodekart.com
